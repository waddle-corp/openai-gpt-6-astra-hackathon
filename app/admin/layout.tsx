import './admin.css';
export const metadata = {
  title: 'Pay with Feedback — Merchant workspace',
  robots: { index: false, follow: false },
};
export default function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
